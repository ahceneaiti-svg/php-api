apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: user-api
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Uses minikube's default StorageClass (standard / hostpath).
